{
  "name": "my-app",
  "version": "1.0.0",
  "scripts": {
    "server": "cd server && nodemon server.js" ,
    "watch-client": "cd client && npm run watch",
    "dev": "concurrently \"npm run server\" \"npm run watch-client\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "nodemon": "^2.0.12",
    "watch": "^1.0.2" 
  }
}
